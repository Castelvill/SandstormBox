Camera:
Objects.agg(Camera.inView)
Objects.agg(Camera.outsideView)

Mouse:
Objects.agg(Mouse.collision_with)
Objects.agg(Mouse.outside_of)
Objects.agg(Mouse.pressed_on)
Objects.agg(Mouse.released_on))

Objects:
Layers.agg(...)
Objects.agg(...)
Objects.agg(id == {string})
Objects.agg(id in {vector<string>})
Objects.agg(group == {string})
Objects.agg(group in {vector<string>})
Objects.agg(pos in {vec2d, vec2d})
Objects.agg(isScaledFromCenter == {bool})
Objects.agg(isAttachedToCamera == {bool})
Objects.agg(canBeSelected == {bool})
Objects.agg(isScrollable == {bool})

Variables:
Objects.agg(...).Var.agg(id == {string} && value {op} {value})
Objects.agg(...).Var.agg({string} {op} {value})

Collision:
Objects.agg(Me.outside_of) 
Objects.agg(Me.overlaping_with)
Objects.agg(Me.collision_with)
Objects.agg(Me.collision_on_X_with)
Objects.agg(Me.collision_on_Y_with)

General:
Objects.agg(...).size()