func createLevel(Camera MainCamera){
	Layer Level1 = Layers.new("Level1");
	MainCamera.connectLayer(Level1);
}

event setupGame(on_init){
	MainCamera = Cameras.new("GameCamera");
	createLevel(MainCamera);
}

func mover(){
	Camera MainCamera;
	spawnNewWindow(MainCamera);
	spawnMover(MainCamera);
}