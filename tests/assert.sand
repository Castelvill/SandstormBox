import "tests/test_template.sand"

start main()
    triggers on_init
    run startTest()

    assert NULL NULL
    assert a NULL
    assert NULL a
    assert 0 0
    assert 5 5
    
    int a 0
    assert a 0
    assert 0 a
    = a 12
    assert a 12
    assert 12 a

    run endTest()
end
