start main()
    int x 0
    if([x] [10] <)
        = x 100
    else_if([x] [50] <)
        = x 200
    else
        = x 300
    end_if
    print _ _ x "\n"
end

/*start main()
    triggers on_init
    int x 10
    run event_if(x)
    print _ _ x "\n"
    exit
end

start event_if(int & x)
    if([x] [10] <)
    else event_else_if(x)
    = x 100
end

start event_else_if(int & x)
    if([x] [50] <)
    else event_else(x)
    = x 200
end

start event_else(int & x)
    = x 300
end*/