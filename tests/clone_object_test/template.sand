@trigger on_init
start onInit()
    access myName [self.id]
    if(myName == "Template")
        assert 1 0
    end

    ++ var0
    assert var0 16

    decrementLock()

    //print "Hello from " myName ": " var0 "\n"
end

start decrementLock()
    var idleLock 0
    first self_layer _ _ _ _ Object _ objects
    find_by_id_2 objects testObjName testObj
    first testObj _ _ var idleLock _ _ idleLock
    -- idleLock
end