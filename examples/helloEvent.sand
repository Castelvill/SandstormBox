//Create a simple single-use event without closing the engine.
//To close the engine use CTRL+ESC shortcut, "power_off" instruction or just click the exit button.

start helloWorldEvent
    triggers each_iteration
    print "Hello, World!\n"

    var KEY_LCTRL 217
    var KEY_ESC 59
    
    delete_this_event
end

start exitProgram
    triggers key_pressed
    if([key_pressed KEY_LCTRL] [key_pressed KEY_ESC] &&)
    power_off
end