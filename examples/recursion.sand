start printNumbers(int n)
    if(n 0 <=)
        return
    end_if
    print n "\n"
    -- n
    run printNumbers(n)
end

start main()
    triggers on_init
    int n 5
    printNumbers(n)
    exit
end